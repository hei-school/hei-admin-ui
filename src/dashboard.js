import * as React from "react";
import { Card, CardHeader } from '@material-ui/core';

const Dashboard = () => (
    <Card>
        <CardHeader title="HEI Admin" />
    </Card>
);
export default Dashboard;